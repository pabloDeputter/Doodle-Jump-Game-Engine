#include "src/Entity/Platform.h"
#include "src/Entity/Player.h"
#include "src/Game.h"
#include "src/Observer/Subject.h"
#include "src/Utils/Random.h"
#include "src/Utils/Stopwatch.h"
#include "src/World.h"
#include <iostream>
#include <memory>
#include <thread>

/* TODO
 * Compile different directories, such Utils should never be compiled again
 * Observer class should be base class of all entities?
 */

int main(int argc, char* argv[])
{
        //
        //        std::unique_ptr<World> w = std::make_unique<World>();
        //        std::shared_ptr<Model::Entity> p0 = std::make_shared<Model::Platform>();
        //        std::shared_ptr<Model::Entity> p1 = std::make_shared<Model::Platform>();
        //        std::shared_ptr<Model::Entity> p2 = std::make_shared<Model::Platform>();
        //        std::shared_ptr<Model::Entity> p3 = std::make_shared<Model::Platform>();
        //

        //        Game* a = new Game();
        //        a->run();

        //        std::cout << "Hello world\n";
        return 0;
}